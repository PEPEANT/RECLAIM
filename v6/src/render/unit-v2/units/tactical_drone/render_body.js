// Body rendering for: tactical_drone
(function attachUnitRenderV2Body_tactical_drone(globalScope) {
    'use strict';

    function drawBody(unit, ctx) {
        if (!ctx) return;
        // TODO: implement body rendering for tactical_drone
    }

    globalScope['UnitRenderV2Body_tactical_drone'] = {
        drawBody: drawBody
    };
})(typeof window !== 'undefined' ? window : globalThis);
