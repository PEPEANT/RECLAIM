// Body rendering for: recon
(function attachUnitRenderV2Body_recon(globalScope) {
    'use strict';

    function drawBody(unit, ctx) {
        if (!ctx) return;
        // TODO: implement body rendering for recon
    }

    globalScope['UnitRenderV2Body_recon'] = {
        drawBody: drawBody
    };
})(typeof window !== 'undefined' ? window : globalThis);
