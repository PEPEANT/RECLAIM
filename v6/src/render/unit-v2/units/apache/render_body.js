// Body rendering for: apache
(function attachUnitRenderV2Body_apache(globalScope) {
    'use strict';

    function drawBody(unit, ctx) {
        if (!ctx) return;
        // TODO: implement body rendering for apache
    }

    globalScope['UnitRenderV2Body_apache'] = {
        drawBody: drawBody
    };
})(typeof window !== 'undefined' ? window : globalThis);
