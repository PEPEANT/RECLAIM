// Body rendering for: aa_tank
(function attachUnitRenderV2Body_aa_tank(globalScope) {
    'use strict';

    function drawBody(unit, ctx) {
        if (!ctx) return;
        // TODO: implement body rendering for aa_tank
    }

    globalScope['UnitRenderV2Body_aa_tank'] = {
        drawBody: drawBody
    };
})(typeof window !== 'undefined' ? window : globalThis);
