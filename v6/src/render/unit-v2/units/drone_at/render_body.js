// Body rendering for: drone_at
(function attachUnitRenderV2Body_drone_at(globalScope) {
    'use strict';

    function drawBody(unit, ctx) {
        if (!ctx) return;
        // TODO: implement body rendering for drone_at
    }

    globalScope['UnitRenderV2Body_drone_at'] = {
        drawBody: drawBody
    };
})(typeof window !== 'undefined' ? window : globalThis);
