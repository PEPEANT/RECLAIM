// Body rendering for: apc
(function attachUnitRenderV2Body_apc(globalScope) {
    'use strict';

    function drawBody(unit, ctx) {
        if (!ctx) return;
        // TODO: implement body rendering for apc
    }

    globalScope['UnitRenderV2Body_apc'] = {
        drawBody: drawBody
    };
})(typeof window !== 'undefined' ? window : globalThis);
