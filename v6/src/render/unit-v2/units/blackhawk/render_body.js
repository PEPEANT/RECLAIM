// Body rendering for: blackhawk
(function attachUnitRenderV2Body_blackhawk(globalScope) {
    'use strict';

    function drawBody(unit, ctx) {
        if (!ctx) return;
        // TODO: implement body rendering for blackhawk
    }

    globalScope['UnitRenderV2Body_blackhawk'] = {
        drawBody: drawBody
    };
})(typeof window !== 'undefined' ? window : globalThis);
