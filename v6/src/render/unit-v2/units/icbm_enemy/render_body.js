// Body rendering for: icbm_enemy
(function attachUnitRenderV2Body_icbm_enemy(globalScope) {
    'use strict';

    function drawBody(unit, ctx) {
        if (!ctx) return;
        // TODO: implement body rendering for icbm_enemy
    }

    globalScope['UnitRenderV2Body_icbm_enemy'] = {
        drawBody: drawBody
    };
})(typeof window !== 'undefined' ? window : globalThis);
