# RECLAIM 안정화 개선 계획서 (2026-02-21)

## 0) 이번 점검 결과 (실측)
- 스모크 테스트 `node tools/smoke/smoke_runner.cjs` 통과 (`SMOKE_OK`, `4xx/5xx=0`).
- JS 구문 점검 통과(전 파일 파싱 기준 문법 오류 0건).
- 치명 경계 버그 1건 즉시 보강 완료:
  - 강제 업데이트 차단 상태에서 부트게이트/서비스워커가 뒤에서 동작할 수 있는 경로를 차단.
  - 반영 파일: `index.html`.

## 1) 목적
- 구버전 캐시 잔재, 유닛 렌더/발사 상태 꼬임, 접지(바닥 기준) 불일치를 우선 안정화한다.
- "기능 추가"보다 "재현 가능한 버그 제거 + 배포 신뢰성"을 먼저 확보한다.

## 2) 현재 진단 요약
- 배포 반영 지연의 주원인은 서버가 아니라 브라우저 캐시/서비스워커/로컬 스토리지 잔재다.
- 자주포(SPG)는 발사 자세/이동 자세 분리가 들어갔지만, 프레임 경계에서 발사 게이트가 흔들릴 수 있어 보강이 필요했다.
- 전차(MBT) 포구 이펙트는 반동 기반 잔류 계산 때문에 잔상처럼 보일 가능성이 있었고, 발사 프레임 기준으로 고정했다.
- 험비 스케일과 기갑 유닛 feet 오프셋이 서로 달라 접지 체감이 들쭉날쭉했다.

## 3) 우선순위
## P0 (즉시, 1~2일)
- 캐시 강제 갱신 흐름 고정
  - `version.json` 기반 버전 가드
  - 구버전 감지 시 서비스워커/캐시스토리지 정리
  - 구버전 차단 오버레이 + 최신 주소 이동 제공
- MBT 포구 잔상 재발 방지
  - 포구 플래시를 "발사 프레임 기반"으로만 표시
- SPG 발사 게이트 고정
  - 이동 중 발사 금지 우선
  - 포신 상승 전 발사 금지

## P1 (단기, 3~5일)
- 접지(바닥 기준) 교정
  - `humvee/apc/mbt/spg/aa_tank` feet 오프셋 샘플 전수 점검
  - 맵별(평지/도시/사막) 기준 캡처 비교
- SPG 체감 튜닝
  - 상승/하강 속도, 발사 가능 각도, 고각 쿨타임을 표준값으로 확정
- 수동 조작 유닛 입력 안정화
  - 선택 해제/재선택 시 수동 조준 상태 초기화 검증

## P2 (중기, 1~2주)
- 배포 자동화
  - 배포 시 `version.json`, `version.js`, `index.html`의 빌드 문자열 자동 동기화
  - 수동 버전 누락으로 인한 오탐 차단
- 회귀 테스트 루틴 최소화
  - "배포 전 5분 체크리스트" 스크립트/문서화

## 4) 구현 체크리스트
- [x] 버전 가드 동작 확인 (기존 탭/시크릿 탭/하드 리프레시)
- [ ] 구버전 차단 오버레이 표시 확인
- [ ] 최신 주소 이동 버튼/자동 이동 확인
- [ ] MBT 포구 플래시 10회 연속 발사 잔상 유무 확인
- [ ] SPG 이동 중 발사 차단 확인
- [ ] SPG 정지 후 포신 상승 완료 뒤 발사 확인
- [ ] 험비/전차/자주포/대공전차 바닥 접지 픽셀 오차 기록

## 5) QA 시나리오
- 시나리오 A: 같은 브라우저에서 기존 탭 유지 후 재배포
  - 기대값: 구버전 차단/최신 이동 안내가 즉시 뜬다.
- 시나리오 B: 자주포 선택 후 이동-정지-사격 반복
  - 기대값: 이동 중 발사 안 되고, 포신 올린 뒤 발사된다.
- 시나리오 C: 전차 20발 연속 발사
  - 기대값: 포구 이펙트 잔상 없이 프레임 내 소거된다.
- 시나리오 D: 연병장 프로필/인게임 전장 모두에서 기갑 접지 비교
  - 기대값: 바닥 기준이 유닛 간 유사하게 맞는다.

## 6) 리스크 및 대응
- 리스크: 빌드 버전 문자열을 수동으로 여러 파일에 넣으면 누락 가능성 높음
  - 대응: 배포 스크립트에서 버전 문자열 단일 생성 후 3개 파일 동시 치환
- 리스크: 강한 캐시 무효화는 초기 로딩 트래픽 증가
  - 대응: 안정화 완료 후 `index/version`만 no-store, 나머지는 짧은 캐시로 조정

## 7) 완료 기준 (Definition of Done)
- 기존 탭에서 구버전 잔재로 플레이 진입되는 케이스 0건
- MBT 포구 잔상 재현률 0%
- SPG 발사 게이트(이동 중 금지 + 상승 후 발사) 재현률 100%
- 주요 기갑 유닛 바닥 접지 오차가 허용 범위(±2px) 내
