// Body rendering for: stealth_drone
(function attachUnitRenderV2Body_stealth_drone(globalScope) {
    'use strict';

    function drawBody(unit, ctx) {
        if (!ctx) return;
        // TODO: implement body rendering for stealth_drone
    }

    globalScope['UnitRenderV2Body_stealth_drone'] = {
        drawBody: drawBody
    };
})(typeof window !== 'undefined' ? window : globalThis);
